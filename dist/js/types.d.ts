/**
 * Re-export all types from the types folder for backward compatibility
 * This ensures existing imports continue to work while we transition to the new structure
 */
export * from "./types/index";
