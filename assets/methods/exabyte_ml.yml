!combine
name:
  template: '{{ units[0]["name"] }}'
forEach:
  - key: units
    action: push
    values: !include assets/methods/units/regression.yml
config:
  schema: !esse method/categorized-method
